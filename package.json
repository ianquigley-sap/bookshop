{
  "name": "bookshop",
  "description": "Generated by cds init",
  "repository": "<Add your repository here>",
  "license": "<Add your license here>",
  "version": "1.0.0",
  "engines": {
    "node": "^8.9"
  },
  "files": [
    "srv",
    "db"
  ],
  "dependencies": {
    "@sap/cds": "^3.10.0-20190513082928-SNAPSHOT",
    "chai": "^4.2.0",
    "express": "^4.16.4",
    "ls": "^0.2.1",
    "npm-watch": "^0.6.0",
    "serverless-http": "^2.0.1"
  },
  "watch": {
    "run_cds": {
      "patterns": [
        "./db/*.*"
      ],
      "extensions": "cds,csv",
      "quiet": false
    }
  },
  "scripts": {
    "watch": "npm-watch run_cds",
    "run_cds": "cds deploy && cds run",
    "start": "cds run",
    "build": "cds build/all --clean",
    "deploy": "cds deploy"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite",
        "model": [
          "db",
          "srv"
        ],
        "credentials": {
          "database": "my.db"
        }
      }
    }
  },
  "devDependencies": {
    "randomstring": "^1.1.5"
  }
}
